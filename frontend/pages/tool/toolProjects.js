import Projects from "@/components/WorkManagementTool/Project/Projects";

const ToolProjects = ({ user }) => {
    return (<>
        <Projects user={user} />
        </>
        );
};

export default ToolProjects;